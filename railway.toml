[build]
builder = "nixpacks"
buildCommand = "cd neverness-builds && npm install && npm run build"

[deploy]
startCommand = "cd neverness-builds && npm run preview -- --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
