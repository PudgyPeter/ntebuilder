[build]
builder = "DOCKERFILE"
dockerfilePath = "neverness-builds/Dockerfile"

[deploy]
startCommand = "sh -c 'serve /app/dist -s -l tcp://0.0.0.0:${PORT}'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
